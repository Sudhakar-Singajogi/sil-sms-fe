import { createSlice } from "@reduxjs/toolkit";

const UserAuth = createSlice({
    name:'UserAuth',
    initialState:{
        school:{},
        user:{}
    },
    reducers:{},
    extraReducers:(builders) => {

    }

})